# Automatically activate virtual environment when entering directory
source .venv/bin/activate

# Optional: Set Zscaler certificate if needed
# Uncomment the line below if you're behind Zscaler proxy
# export REQUESTS_CA_BUNDLE=Zscaler.pem